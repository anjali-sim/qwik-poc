import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div class="w-64 bg-gray-100 p-4">
      <h2 class="font-bold mb-2">Chat History</h2>
      <p>(Extend with sessions later)</p>
    </div>
  );
});